# Project Rules

- **Deployment Constraint**: Do NOT run production deployment commands (`npm run deploy:prod`, `firebase deploy -P production`, or git push to prod) automatically after making code changes. Always verify locally and wait for explicit confirmation/request from the user before deploying to production.
